---
title: (TUTORIAL 3) Introduction to Linux kernel build configuration and modules
date: 2026-03-11
categories: [Patch (Kernel), Tutorial]
tags: [tutorial, kernel]     # TAG names should always be lowercase
---
# Objective:
Make a simple Linux kernel module and create build configurations for new kernel features. More specifically,
two modules were added to the kernel.

# Problems:
I had a problem with using kw ssh to connect to the VM. For some reason, I needed to provide the IP of the VM again (even though I had already set it as default). Since I wasn't quite familiar with the tools, I assumed it was a problem with kw and reinstalled it, which took me some time. Reinstalling it didn't fix anything, because the problem was that kw didn't know the IP of the VM anymore, so I had to do some fiddling until finally realizing I had to set up the IP again.


That being said, no major problems with Tutorial 3, in question. 

# Reference:
[(Tutorial 3 - 11/03) Introduction to Linux kernel build configuration and modules](https://flusp.ime.usp.br/kernel/modules-intro/).
